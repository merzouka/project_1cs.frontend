import { DesktopNav } from "./desktop-nav";
import { MobileNav } from "./mobile-nav";

export const NavBar = () => {
    return (
        <>
            <DesktopNav />
            <MobileNav />
        </>
    );
}
